# 进度条拖拽功能说明

## ✅ 功能概述

Banana Player 已经完全支持进度条拖拽功能，用户可以快速跳转到视频的任意位置。

## 🎯 功能特性

### 1. 实时拖拽
- **拖动时预览**: 在暂停状态下，拖动进度条可以实时预览帧画面
- **平滑跳转**: 释放鼠标后自动跳转到指定位置
- **边界保护**: 自动处理超出范围的情况，不会崩溃

### 2. 智能缓存
- **LRU 缓存**: 最近访问的帧会缓存在内存中
- **快速响应**: 缓存命中时跳转几乎瞬时完成
- **内存控制**: 默认缓存 60 帧，最大内存 500MB

### 3. 防递归保护
- **seeking 标志**: 防止进度条更新时的递归循环
- **线程安全**: 支持多线程环境下的安全操作

## 📖 使用方法

### 方法一：鼠标拖拽
```
1. 打开 .ban 视频文件
2. 找到下方的进度条
3. 用鼠标左键点击并拖动滑块
4. 释放鼠标，视频会跳转到该位置
```

### 方法二：点击跳转
```
1. 直接点击进度条的任意位置
2. 视频会自动跳转到点击位置
```

### 方法三：配合播放控制
```
播放中拖拽：
- 拖动时视频会暂停
- 释放后继续从新位置播放

暂停时拖拽：
- 拖动时可以预览帧画面
- 释放后停在当前帧
```

## ⚙️ 技术实现

### 核心代码

```python
# 进度条创建 (src/player.py:209-211)
self.progress = ttk.Scale(
    control_frame, 
    from_=0, 
    to=100, 
    orient=tk.HORIZONTAL,
    command=self.seek_video  # 绑定回调函数
)

# 跳转函数 (src/player.py:591-597)
def seek_video(self, value):
    """跳转到指定位置"""
    if self.decoder and not self.seeking:
        self.seeking = True
        self.current_frame_idx = int(float(value))
        self.display_frame(self.current_frame_idx)
        self.seeking = False
```

### 防递归机制

```python
# display_frame 中的进度条更新 (src/player.py:520-524)
if not self.seeking:
    self.seeking = True
    self.progress.set(frame_idx)  # 这会触发 command 回调
    self.seeking = False  # 但因为 seeking=True，所以不会重复
```

### 流式解码集成

```python
# 使用流式解码器获取帧 (src/player.py:466-468)
frame = self.decoder.get_frame(frame_idx)
if frame is None:
    return

# 自动从缓存或磁盘加载
# - 缓存命中：瞬时返回
# - 缓存未命中：从磁盘加载并加入缓存
```

## 🎨 界面说明

### 进度条位置
- **位置**: 控制面板中央
- **宽度**: 600 像素
- **范围**: 0 到 (总帧数 - 1)

### 时间显示
```
格式: MM:SS / MM:SS
示例: 00:00 / 01:30
```

### 控制按钮
- **[▶ 播放]**: 开始/暂停播放
- **[■ 停止]**: 停止播放并回到开头
- **进度条**: 拖拽调整播放位置

## 📊 性能数据

### 测试结果 (vis.ban - 500帧视频)

| 操作 | 响应时间 | 说明 |
|------|----------|------|
| 首次跳转 | ~50-200ms | 需要从磁盘加载帧 |
| 缓存命中 | <5ms | 帧已在内存中 |
| 连续跳转 | 平均 50ms | 混合缓存和磁盘加载 |

### 缓存效果
- **随机跳转**: ~30-65% 命中率
- **顺序播放**: ~95%+ 命中率
- **内存占用**: 13-50 MB（默认配置）

## 🔧 配置选项

### 修改缓存大小

在 `src/player.py` 的 `open_video()` 方法中修改：

```python
# 默认配置
self.decoder = StreamingDecoder(
    file_path,
    buffer_size=60,          # 缓存帧数
    buffer_memory_mb=500     # 最大内存 (MB)
)

# 配置示例
self.decoder = StreamingDecoder(
    file_path,
    buffer_size=120,         # 更多缓存（更快，但更多内存）
    buffer_memory_mb=1000    # 更多内存限制
)
```

### 配置建议

| 视频大小 | buffer_size | buffer_memory_mb | 说明 |
|----------|-------------|-----------------|------|
| 小视频 (<1000帧) | 30-60 | 200-500 | 默认配置 |
| 中等视频 (1000-5000帧) | 60-120 | 500-1000 | 增加缓存 |
| 大视频 (>5000帧) | 120-240 | 1000-2000 | 大缓存 |

## ⚡ 优化技巧

### 1. 提高跳转速度
```python
# 增加缓存大小
buffer_size=120  # 默认 60
```

### 2. 减少内存使用
```python
# 减少缓存大小
buffer_size=30   # 默认 60
buffer_memory_mb=250  # 默认 500
```

### 3. 查看缓存统计
```python
# 在播放器中添加调试输出
stats = self.decoder.get_stats()
print(f"缓存命中率: {stats['buffer_stats']['hit_rate']:.1f}%")
print(f"内存使用: {stats['buffer_stats']['memory_mb']:.1f} MB")
```

## 🐛 常见问题

### Q1: 拖动进度条卡顿？
**A**: 可能是磁盘读取慢，可以：
- 增加缓存大小
- 检查磁盘性能
- 使用 SSD

### Q2: 拖动后画面黑屏？
**A**: 可能是帧解码失败，检查：
- 视频文件是否损坏
- 帧索引是否超出范围

### Q3: 内存占用过高？
**A**: 调整配置：
```python
buffer_memory_mb=200  # 减少到 200MB
```

## 🎓 高级功能

### 键盘快捷键配合
```
左右箭头: 逐帧前进/后退
Ctrl+左右: 快进/快退 10%
空格: 播放/暂停
```

### 快速定位
```
1. 先播放到大致位置
2. 暂停播放
3. 精确拖动进度条
4. 释放鼠标完成定位
```

## 📝 更新日志

### v2.0 (当前版本)
- ✅ 完整的进度条拖拽支持
- ✅ 流式解码集成
- ✅ 智能帧缓存
- ✅ 防递归保护
- ✅ 边界条件处理

### v1.0 (初始版本)
- ✅ 基本进度条显示
- ✅ 自动进度更新

## 🔗 相关文件

- `src/player.py` - 播放器主界面和拖拽逻辑
- `src/frame_buffer.py` - 流式解码器和帧缓存
- `src/codec.py` - 视频编解码

## 💡 使用提示

1. **首次使用**: 建议先测试小视频文件
2. **大视频**: 考虑增加缓存大小
3. **网络存储**: 建议复制到本地后再播放
4. **快速定位**: 组合使用键盘快捷键和进度条

## 🚀 未来改进

计划中的功能：
- [ ] 拖拽时显示缩略图预览
- [ ] 可视化缓存在进度条上
- [ ] 标记点快速跳转
- [ ] A-B 段重复播放

---

**版本**: 2.0  
**最后更新**: 2026-01-12  
**作者**: Banana Player Team
